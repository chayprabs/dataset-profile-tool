export function SeoStrip() {
  return (
    <section aria-label="Product summary" className="seo-strip">
      <p>
        Profile CSV, Parquet, JSONL, Avro, and SQLite datasets online — infer JSON Schema,
        column stats, nulls, cardinality, PII hints, and anomalies.
      </p>
      <p>
        Compare two snapshots for schema drift, export reports, and share read-only links —
        streaming through DuckDB without loading the full file into your browser.
      </p>
    </section>
  );
}
